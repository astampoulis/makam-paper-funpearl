box: ablu/ubuntu-texlive-full

dev:
  steps:
    - install-packages:
        packages: inotify-tools pandoc
    - internal/watch:
        code: make watch
    # - internal/shell:
    #     cmd: /bin/bash

build:
  steps:
    - install-packages:
        packages: inotify-tools pandoc
    - script:
        name: Build everything
        code: make
