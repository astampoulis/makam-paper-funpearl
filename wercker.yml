box: ablu/ubuntu-texlive-full

dev:
  steps:
    - install-packages:
        packages: inotify-tools pandoc
    - script:
        code: if [[ -z "$JUST_COMPILE" ]]; then make watch; else make; fi
    # - internal/shell:
    #     cmd: /bin/bash

build:
  steps:
    - install-packages:
        packages: inotify-tools pandoc
    - script:
        name: Build everything
        code: make
